# OralScan Backend

This repository now contains only the backend application in the repository root.

How to run (example):

- Install dependencies: `npm install`
- Start: `npm start`

If you need the frontend, it was removed from this repo.
